<% if !user_signed_in? %>
    <h1>Welcome</h1>
    <p>To use chat you have to be signed in so go go:</p>
    <p>
      <%= link_to "Sign up", new_user_registration_path, :class => "btn btn-primary btn-lg active" %> or
      <%= link_to "Sign in", new_user_session_path, :class => "btn btn-primary btn-lg active"%>
    </p>

<%else%>

    <h1>Welcome</h1>
    <p>To use chat go:</p>
    <p><%= link_to "Vos Chat", chat_path, :class => "btn btn-primary btn-lg active" %></p>

<%end%>